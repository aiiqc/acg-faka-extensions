<?php
declare(strict_types=1);

// Admission runs in the manager's mandatory pre-controller guard, not in a
// short-circuitable manifest callback. This package supplies its settings card.
return true;
