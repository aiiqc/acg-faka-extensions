# 项目状态与验证边界

## 当前状态：2026-09-25 Pika 1.1.7 源码更新

本批只修复主题中的店名换行及窄屏布局，补充官方公告外链配置教程与真实合成主题演示。完整名称继续经过模板转义；不改官方核心、过滤器、业务插件、数据库或配置格式。主题为 `1.1.7`，其他组件及套装版本保持。

main 追加普通源码提交，不导入私人历史，不新增 tag／Release。旧 `v0.1.0-preview.1` 的固定附件仍为主题 `1.1.6`，不含本修复；下方首次公开记录是历史证据，不能外推为新包已发布。实际提交看本仓库 Git 历史，不在提交内写自身 SHA。

本地验证 PASS：12 项主题合同和 PHP 元数据行为；同一浏览器测试在旧 CSS 复现隐藏／裁切，新 CSS 的 70 组店名、126 个导航场景、336 个库存场景和 48 个购买夹具全部通过，浏览器错误与未预期请求均为零。使用固定官方 3.7.9 的真实主题模板及 Bootstrap/Pika CSS/JS；覆盖 320／375px、经典滚动条、登录／未登录及中英长短名称和无空格串。独立静态审查未发现实质问题。这些数字不是交易或商品验收数量。

演示图仅用虚构商品、价格与中性源素材，不读取生产截图、联系人或货源。目标站部署、手机实机与真实交易不由上述证据替代；本任务不执行生产变更。

## 历史状态：2026-09-24 首次预发布提交前截点

首次公开目标为 [aiiqc/acg-faka-extensions](https://github.com/aiiqc/acg-faka-extensions)，预发布标识 `v0.1.0-preview.1`。本页记录初始提交前的事实：空公库与私密漏洞报告已建立并核验；将从独立快照创建全新历史，不导入原私人仓库历史。最终 commit、tag、固定制品及下载校验以[预发布页](https://github.com/aiiqc/acg-faka-extensions/releases/tag/v0.1.0-preview.1)实际内容为准，不在提交内编造自身SHA或提前宣称匿名下载通过。候选外的证据、运行资料和旧候选不属于公开文件。

套装仍为 `0.1.0-dev`：PikaSupplySync `1.1.17`、PikaCatalogHub `0.6.9`、PikaOrderReturnWait `1.1.0`、Pika 主题 `1.1.6`、PikaBEpusdtAdapter `0.1.2`。1.1.17 在既有库存保护上增加有界诊断，不改同步行为、重试／预算、UI 或持久状态。支付适配器的私有展示 URL 继续留空；默认加价 0%，无实际货源或运营公告。

新安装只推荐固定官方 Acg-Faka `3.7.9 / 5120942d2c13ac900d614b09cfd6fbf672b62840`。旧兼容指纹保留，不是旧核心安全维护承诺。先看[新手路径与截图](USER_GUIDE.md)，再按[安装教程](USER_INSTALL.md)核对初始化、权限、备份及升级／恢复；获取命令仅在固定附件存在且SHA已核实后使用。预览版不是稳定版或生产许可。

## 首次预发布的历史证据与不能外推的边界

| 层次 | 结论及限制 |
|---|---|
| 固定源码与本地相关回归 | 1.1.17 的诊断、库存、字段选择及相关合同已有通过证据。本次只复用逐字一致输入；不是全仓测试全绿 |
| 固定私人制品生命周期 | 同一源码制品在固定官方 3.7.9 的原生 Git 隔离环境完成 install／verify／doctor／配对 restore；无真实上游／数据库连接，不等于本候选的最终公开包 |
| 公开候选隔离生命周期 | PASS：冻结公开候选实际完成安装前 doctor → install（155 staged／154 managed）→ verify／installed doctor → 配对 receipt restore（154）→ 恢复后 doctor；真实固定官方 Git、容器禁网、无数据库连接。本目录仅增加导览图片及修改 README／docs，安装工具、载荷及其余输入逐字不变；不是最终公开 commit／下载包或生产验证 |
| 历史核心升级／状态接续 | 固定基线的旧安装恢复、新安装、兼容状态接续及合成迁移／恢复已有独立证据，不保证任意站点可灾备恢复 |
| 限定目标环境非交易运行 | 固定 3.7.9 + Supply1.1.17 已有部署、系统与真实浏览器非交易检查；自然同步既有成功，也出现外部 522／超时。不是上游故障治愈、全部商品覆盖或本候选已部署 |
| 本次文档与展示 | 管理器 16 个合成浏览器场景及两张图沿用原证据；本轮当前 Hub 0.6.9 的 9 项合成检查通过，新增接入／确认／任务三图。两批均拦截全部网络、外部发送 0；旧 0.6.0 两图原样保留为历史参考。不是完整官方后台启动链、mirror／补处理或真实入库验收；完整缺项见导览 |
| 库存细分生产分支 | NOT EVALUATED：动作计数、总量熔断及一次清零不能代替所有分支覆盖 |
| 升级后真实交易、独立实时报价算术、全商品／全移动端 E2E、完整灾备恢复 | NOT RUN；旧支付反馈、本地测试、截图、健康与运行日志均不能替代 |
| 首次公开发行 | 本页为提交前截点：公库及私密漏洞报告已核；单root commit、远端main／tag、制品及匿名获取须在后续发布步骤分别读回，不在此预写PASS。最终身份以预发布页为准 |

已知旧夹具问题保留：`tests/local-catalog-preview-diagnostics-behavior.php` 的 ConfigRepository 替身缺少 `categoryMode()`，在目录解析前失败。本轮不修、不重跑，也不把它描述为本次新增产品缺陷。历史 3.6.4 分类镜像、BE 新入口在 3.6.4／3.7.0 的动态证据未补齐。

## 首次预发布的文件与历史策略

公开内容仅为审核后的独立快照及本次必要公开文档收尾。原私库历史含生产信息及本机路径，保持私有，不复制其 `.git`、refs、reflog 或不可达对象。新历史仅建立一个无父初始提交，使用维护者既有GitHub noreply身份；不在此公开邮箱值。文件集合、实际commit和下载制品是不同身份，发布步骤须分别核对。

规则扫描和目视检查只能证明所列范围未发现命中，不保证“绝无秘密”或二进制逐帧／隐写审计。不要把候选外的清单、测试 JSON、生产回执或本机路径放进发行包。

## 素材权利与安全报告

代码 MIT 不覆盖四项主题媒体。维护者选择在本次预发布保留 logo、favicon、背景海报及视频原字节与已确认出处；再分发权利仍 UNKNOWN。该选择、免费或署名均不是第三方授权，不提供权利已解决的法律结论。原素材说明的独立许可提醒保持；下游使用或再分发前仍须核实权利。见[第三方声明](../THIRD_PARTY_NOTICES.md)和[素材说明](../themes/Pika/Assets/README.md)。

公开仓库的 GitHub Private Vulnerability Reporting 已启用且读回为true；实际入口与脱敏要求见[SECURITY.md](../SECURITY.md)。没有发送测试漏洞报告，不承诺响应时限，也不公布猜测的邮箱。

## 首次预发布时的下一步（历史原文）

本地安装恢复与导览收尾已完成。本次在另复制的发布工作区创建一次初始提交并发布 `v0.1.0-preview.1`，核对远端main、tag、固定包与匿名获取后停止；冻结候选和旧证据不改写。此提交内的状态是发布前截点，后续结果看实际预发布页，不为改变时态再增加提交。公开流程不部署、不运行真实同步／入库／交易。贡献和更新记录见 [CONTRIBUTING.md](../CONTRIBUTING.md) 与 [CHANGELOG.md](../CHANGELOG.md)。
