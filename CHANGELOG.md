# 更新记录

本文件记录首次公开快照的用户可见功能和升级注意，不是部署或交易验收凭证。提交前截点与证据边界见[项目状态](docs/PROJECT_STATUS.md)，最终发布身份以预发布页为准。

## 2026-09-26 — 店铺共享准入 0.1.0

- 新增默认关闭的 `PikaSharedAccess` 设置卡，复用原管理器启停与站外 ConfigStore；允许账号与对应 IP 成对匹配，不写真实账号／地址入源码，不新增权限平台或同步系统。
- 仅在真实原生 Shared Authentication／Commodity 控制器调用前强制准入；基于已验签用户和 REMOTE_ADDR，不依据 URI 正则或客户端转发头。启用后的空／坏名单拒绝共享请求，bool／Stock 不绕过，原生签名与其他权限不放宽。
- 配置损坏时保留后台停用入口，普通页面、User、后台登录、Shared Plugin face 和支付回调不属于此门；Supply 1.1.20、主题及其他组件版本保持。
- 本批为 main 源码更新，不新增 tag／Release、不替换旧附件；安装后默认关闭且名单为空，不包含真实站点配置。源码、隔离验证与目标站点验收分别记录，源码提交身份不改写既有部署回执，也不代表任意站点已经部署。
- 使用方式与来源地址、失败关闭及恢复边界见[店铺共享准入说明](extensions/PikaSharedAccess/Wiki/README.md)。

## 2026-09-25 — PikaSupplySync 1.1.20 字段分层源码更新

- 仅非定向 basic 在既有同步器内按持久 P/P/P/O/M 机会轮转，所有动作共用原 batch 上限；常规动作不下载封面，媒体独立复查。同件常规与媒体各占动作名额，详情同轮复用，已保存的非图片快照不重复写入；full、首次入库与定向完整同步不改。
- 有效非图片字段先保存；媒体在下载前后重新核验当前身份与单品开关，后次事务只写封面。只选图片不整理内部字段；原鉴权、结构、币种、加价、清零、unknown 与配置保护保持。
- 只把既有图片数量／字节配额标明类型，在新路径中按源或整轮暂停后续媒体，后续常规字段可在剩余时间／文本预算内继续。配额数值、timer、HTTP 重试与超时不变，不承诺上游稳定或全量时效。
- 旧 source 状态及 last_result 白名单不变；小型 sidecar 保存媒体尝试游标、机会槽与当前状态绑定，缺失／上下文改变保守重建，损坏明确失败。两文件不是原子提交；1.1.19 忽略 sidecar，回退1.1.17仍须原三个未知计数的精确投影。
- 日志、manager 与后台分开显示非图片保存、媒体复查／刷新／失败／未完成和配额范围，旧记录显示未知；旧保存与失败计数仍按商品去重。本批为 main 源码更新，相关行为、旧状态兼容与原生安装／配对恢复隔离验证通过；不新增 tag／Release、不替换旧附件，生产部署与真实上游同步仍须独立验收，不代表生产已通过。

## Unreleased — 2026-09-25，PikaSupplySync 1.1.19

- 复用现有有界日志与只读后台，最多保留 20 条安全诊断事件。`error_total` 是事件总数，不等于失败商品数 `failed`；截断明确提示，商品只显示编号哈希，货源级记录不伪造商品身份。
- 分开显示 HTTP、业务码、响应数据形状、正文接收字节和固定原因枚举；上游理由均标为“上游返回的声明，未核实根因”。不记录响应原文、请求头、地址或凭据，HTTP 200 不等于业务成功，缺失观测不补为 0。
- curl 计时为同一次尝试开始后的累计里程碑，不能相加或当成独立阶段；DNS 值不含连接策略在 curl 外部执行的查询。最近请求／最近失败及最多三次尝试不是平均值、p95、完整流水或商品覆盖率。
- 同步规则、预算、重试和其他组件保持。1.1.19 与 1.1.18 的 StateStore 格式相同；只有回退到 1.1.17 或更早版本时，才涉及库存保护三个计数字段的受控投影。本批相关 PHP 行为与原生安装恢复隔离门通过，具体范围见项目状态；不新增 tag／Release、不替换旧附件，不外推目标站或生产通过。

## 2026-09-25，PikaSupplySync 1.1.18 源码更新

- 仅原生 type 0、非镜像 basic 自然同步接受明确 `stock:null` 且 `delivery_way:1` 的库存未知状态，保留身份并整件暂停；不当作缺货／无限库存，也不阻塞其他整数库存项的原批次。恢复整数后自然恢复。
- 未知目录项不取详情／图片、不改商品或分类；详情同形空值竞态也在归一化前保护，库存同步关闭不能旁路。未知项从清零比例分子／分母同时排除，保留普通游标，哈希区分 null 与 0。
- 分开显示目录未知、计划保护及实际保护计数，选中保护为 partial。新码可读旧记录；回退到 1.1.17 或更早版本须备份最新状态、仅移除三个新增计数字段并经旧 StateStore 写回核验，保留游标和业务数据。
- Hub、full、mirror、新导入、定向 CLI 和其他协议保持严格；不改官方核心、其他组件、请求预算或重试。本批只追加普通 main 提交，不新增 tag／Release、不替换旧预发布附件；目标站和真实交易须独立验收。

## 2026-09-25，Pika 1.1.7 源码更新

- 完整显示长中文、英文和无空格店名；桌面可换行，手机品牌与账户／菜单分行，不再隐藏名称。保留模板转义、导航和业务逻辑。
- 复用真实主题合成浏览器夹具检查文字、控件边界与响应式导航；增加干净的前台演示图，仅使用虚构数据和中性源素材。
- 补充官方公告外链配置教程：先保存全站允许域名，再恢复公告；只改店名也会重新提交公告。不关闭安全过滤，不修改官方核心。
- 本批仅更新 main 源码，未新增 tag／Release。旧 `v0.1.0-preview.1` 的固定下载附件仍为主题 1.1.6，**不包含本修复**；不要把 main 或自动 Source code 包当作已验证的新固定发行包。

## v0.1.0-preview.1 — 2026-09-24 首次预发布提交前截点

[公开仓库](https://github.com/aiiqc/acg-faka-extensions)与私密漏洞报告已建立；准确 commit、tag、tar.gz 及 SHA256 以[预发布页](https://github.com/aiiqc/acg-faka-extensions/releases/tag/v0.1.0-preview.1)实际内容为准，缺少附件时停止。套装仍为 `0.1.0-dev`，没有稳定版。第三方媒体原样保留，再分发权利 UNKNOWN；不据此声称已取得许可。

组件：PikaSupplySync `1.1.17`、PikaCatalogHub `0.6.9`、PikaOrderReturnWait `1.1.0`、Pika 主题 `1.1.6`、PikaBEpusdtAdapter `0.1.2`。

### 有界诊断（2026-09-24，PikaSupplySync 1.1.17）

- 目录／详情／图片各保留有界计数、最近请求及最近失败摘要，每请求最多记录三次实际尝试；预算来自最近既有检查采样，未知／非法／未采集值不补为零。日志白名单及原大小上限不变，不记录 URL、响应原文、凭据或完整商品流水。
- 保留预算失败作用域及 partial 的安全诊断，避免图片异常跨来源串用请求观察。没有新增日志系统、UI、依赖、状态格式或同步引擎。
- 补清 batch 上限、动作计数与实际变化数、历史 state 与同轮日志、timer OR 语义和分层停止条件。重试、超时、事务、游标、六字段及库存业务保持；不宣称上游慢响应／5xx 被治愈。

### 首次预发布文档与本地验证（2026-09-24）

- 增加入门路径、两张当前管理器截图及三张 Hub 0.6.9 接入／确认／任务截图，均由当前代码配合合成数据生成；逐功能缺项明示，旧 Hub 0.6.0 图片仍标为历史布局。公开候选的固定官方3.7.9禁网安装／核验／配对文件恢复通过；不等于生产或交易验收。
- 修正 smart 别名／mirror 分类、full 模式拒绝和换汇后的 0% 加价说明；复用安装／升级／恢复、安全披露与贡献指南。第三方媒体保持原样及出处，权利未确认。
- 本页的组件历史只整理已存在源码与固定回执的用户可见变化，不为开发节点补造正式 tag／Release；源码对应关系在候选外审计证据中保留，不复制私库 Git 历史。

### 库存清零保护分流（PikaSupplySync 1.1.16）

- 明确零库存独立按既有最小数量和比例阈值保护；目录缺项继续原总量门及阈值以下清零规则，不把缺项当作永久下架。大量明确零库存仍可触发保护，原日志总量语义与定向全源门不变。
- 普通目录库存缺失/null 改为规划前拒绝该源，非法值继续拒绝；保留合法零、数字字符串和整数浮点。共享入口使 CatalogHub 普通分析同样拒绝不完整库存，镜像及 V4 兼容不改。
- 复用原批量、双游标、清零事务、身份/单品开关、补货和竞态保护，无新配置、持久状态或同步引擎。本次公开不包含部署，不据组件版本推定目标站运行状态。

### 官方核心与安装恢复

- 公开新安装只推荐官方 Acg-Faka `3.7.9 / 5120942d2c13ac900d614b09cfd6fbf672b62840`。新增其精确版本、commit 与核心／支付边界指纹；只移植既有三个文件中的扩展钩子，保留官方 Owner 权限、公告净化、可信代理 HTTPS 判定和金额修复。
- 仅 `3.7.9` 新增 CLI 与实际 FPM 的 `bcmath` 要求；`3.6.4`、`3.7.0`、`3.7.5` 仅保留历史兼容与回归记录，不作为新安装建议。固定 Git 身份、文件哈希与安装回执门不放宽。
- 安装器按精确路径规范化官方可变目录，包括安全检查通过后的 `runtime/throttle`；失败回滚保留原内容、所有权与权限。原生 Smarty 特定 `0771` 目录的兼容仅限已验证的 `runtime/view` 范围，不接受任意放宽权限。
- 恢复前保全支付活动日志及明确支持的轮转档，记录文件摘要和原元数据。仅隔离本站精确 logrotate 规则，计划、手动和验证轮转使用同一实际 state；未知条目或身份漂移即停止。`copytruncate` 仍有短暂丢日志窗口。
- 核心升级须先配对 restore，再换完整固定核心、独立核验必要数据库迁移、安装新扩展及显式接续兼容状态。恢复回执不提供数据库回滚，不能用旧 receipt 覆盖新 receipt。详见[安装与恢复教程](docs/USER_INSTALL.md)。

### 货源同步

- 名称、图片、说明、价格、库存、规格提供独立选择；全部不选不请求上游、不写商品。默认加价 `0%`，周期价格沿商品已有规则重算，不逐轮叠加利润。
- 「完整商品配置跟随上游」默认关闭，有独立的显式货源名单；六项全选、扩大同步名单或 CLI 参数均不自动扩大跟随权限。该模式允许覆盖本地 config 的增改删，关闭策略或回退代码不能恢复已覆盖数据。
- `basic` 对已有显式镜像映射的协议 `0` 货源使用严格校验的签名精简目录；能力、认证或结构失败停止本源，不回退大目录。其他货源／模式保持原路径，详情、加价、熔断和写前身份检查仍复用既有同步链。
- 定向 CLI `--only-code-hashes` 只接受一个明确来源的 1–2 个唯一目标，并受已保存模式、六项选择、完整跟随名单及原生单品开关约束。每件写前重查权限，保存后读回 13 个字段才标记 verified；逐件提交，首个失败停止余项，不推进普通轮询游标。
- 新品仍从智能货源中心分析并确认后入库；`basic` 不自动发现新品，不另建同步引擎。

### 智能货源中心

- 新货源默认智能分类；协议 `0` 可在首次入库前显式保留上游真实分类树。镜像以来源 ID 和上游分类 ID 保留完整父链，不插入显示名层、不按同名合并；已有映射不能切换模式。
- 镜像使用同次签名目录请求投影授权商品及必要祖先。精简 v2 只传必要商品字段，严格保留分类、权限、同 owner 父链和独立容量门；不降级、不压平树、不截断祖先。
- 原生目录组和挂商品分类各限 200，必要祖先闭包独立限 2048；全站映射 2048 节点／1 MiB、商品 10000、深度 100、HTTP／快照 16 MiB 等上限分别生效。两端须配对兼容，不把单个上限当作额外全站容量。
- 可选分类图标使用独立签名 v3 元数据，首次新建节点复用安全图片缓存；已有节点不覆盖手动图标。维护入口最多处理 16 个精确映射，先冻结计划和准备图片，再条件更新与有界回滚；不改商品或分类层级。
- 分析只读；确认后后台分批入库，暂停／取消在当前单项结束后生效，取消不删除已导入数据。可恢复失败沿可信快照和检查点继续；补查只处理可信未入库项，不自动续跑、不重做成功项。
- 详情诊断只保留固定类别与有界数值，不保存响应正文、URL 或凭据。明确单件错误可隔离，凭据、全局策略、预算、数据库或未知写结果仍停止；HTTP 200、合法 JSON 和空详情都不证明商品有效或下架。
- 状态 schema 标号相同不代表旧代码能读新字段；发生新状态或业务写入后，禁止仅切旧代码或恢复旧任务覆盖新进度。先停写、对账并保留最新状态。

### 支付与主题

- 支付适配器为新商品／充值交易生成专属通知地址，重新核验本次签名、命名空间、订单号、渠道和金额，再委派官方服务；已付款重送须满足已付状态与支付时间，不重复履约或记账。失败不返回成功应答，不增加第二套账本或数据库表。
- 旧交易的通知地址及显式测试路由不迁移；移除或回退入口前先结清或过期相关在途交易。Token 与站点命名空间继续保存在站外，不进入数据库或公开配置。
- 主题改善窄屏导航、库存标签和横向溢出；购买页不展示销量，保留库存，不改后台统计或排序。余额仅为页面加载时快照，刷新才更新，不写浏览器存储，也不替代服务端支付校验。

### 验证与公开边界

相关源码契约、合成行为及有界隔离已有历史证据，但它们不替代本公开候选的最终制品、部署、长期运行或真实资金／客户流程验收。贡献者须按[贡献说明](CONTRIBUTING.md)区分依赖和测试层，不把跳过项记为通过。

本项目代码采用 MIT；第三方媒体不因此获得再分发许可。本次按维护者选择保留原素材与出处，权利仍 UNKNOWN，不将该选择等同第三方授权；下游使用或再分发前须自行核实，详见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
